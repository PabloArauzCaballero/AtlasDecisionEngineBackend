<!-- GENERADO POR scripts/docs/generate-catalogs.mjs — NO EDITAR A MANO.
     Fuente: src/modules/seeding/. Ejecute `yarn docs:catalog` tras cambiar el código. -->

# Módulo `seeding`


## Responsabilidad

Código: [`src/modules/seeding/`](https://github.com/) · 40 ficheros TypeScript.

No expone endpoints HTTP.

## Autorización

Este módulo no declara roles: o no expone rutas, o son públicas por diseño.

## Códigos de error propios

No lanza `DomainException` propias.

## Clases exportadas

- `SeedingModule`
- `SeedingService`
