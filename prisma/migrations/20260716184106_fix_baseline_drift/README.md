# Migración: corrección de deriva de baseline

Alinea objetos que el generador histórico de línea base dejó distintos del schema. A nivel de
negocio recupera confiabilidad de despliegue; a nivel de sistema corrige nombres, defaults o
restricciones mediante una migración aditiva sin reescribir la historia aplicada.
